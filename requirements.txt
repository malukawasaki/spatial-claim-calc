# requirements.txt — SpatialClaimCalc / congestionSimulator
# Pinned versions for scientific reproducibility.
# Environment: Python 3.10+
# Install: pip install -r requirements.txt

trimesh==4.12.2
numpy==2.0.2
scipy==1.13.1
pygltflib==1.16.5
shapely==2.0.7

# Testing
pytest>=7.0

# Optional: faster mesh processing
# rtree>=1.0
